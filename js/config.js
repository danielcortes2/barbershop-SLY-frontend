/**
 * Configuracion centralizada del frontend.
 * Cambiar API_BASE_URL cuando se despliegue el backend en Render.
 */
const CONFIG = {
    // Cambiar esta URL por la del backend en Render una vez desplegado
    // Ejemplo: 'https://barbershop-sly-backend.onrender.com/api/v1'
    API_BASE_URL: 'http://localhost:9000/api/v1',
};
